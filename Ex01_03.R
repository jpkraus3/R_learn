# R Statistics Essential Training
# Ex01_03
# Taking a first look at the interface

1:250

print("Hello World!")

x <- 1:5  # puts the numbers 1-5 in the variable x
x  # Displays variable x

y <- c(6, 7, 8, 9, 10)

a <- 1  # use  <- and not =
2 -> a  # can go either way

# vector math
x
y
x + y 
x * 2

# Google's R Style Guide - look it up?

# clean up 
rm(x)  # remove an object
rm(x, y)  # remove more than one
rm(list = ls())  # clear entire workspace
